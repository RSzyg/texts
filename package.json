{
  "name": "@texts/monorepo",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./packages/**/dist",
    "build": "pnpm -r --filter='./packages/*' run build",
    "playground": "pnpm -r --filter='./packages/playground' run dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.4",
    "typescript": "^5.0.2"
  }
}
